#include <iostream>
#include <vector>
#include <list>
#include <map>
#include <string>
#include <stack>
using namespace std;

struct BinOp{
public:
    BinOp(BinOp op, string left, string right) {
        op = op;
        left = left;
        right = right;
    }
}
vector<string> TransformBinOp(vector <string> source) {
    
}
void Render(vector <string> source) {

}